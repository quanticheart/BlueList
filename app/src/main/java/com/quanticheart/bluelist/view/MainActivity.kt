package com.quanticheart.bluelist.view

import com.quanticheart.bluelist.databinding.ActivityMainBinding
import com.quanticheart.core.base.activity.BaseActivity

class MainActivity : BaseActivity<MainViewModel, ActivityMainBinding>() {
    override fun view(binding: ActivityMainBinding) {
    }

    override fun viewModel(viewModel: MainViewModel) {
    }
}
